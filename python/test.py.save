from pyiceberg.catalog import load_catalog

cat = load_catalog('resttb')

tbl = cat.load_table(('tpcds', 'catalog_returns'))

scan = tbl.new_scan()

dataset = scan.dataset

dataset.to_table().to_pandas().head()
